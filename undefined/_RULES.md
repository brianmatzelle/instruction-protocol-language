hashtag refers to a new internal, defined instruction for the protocol
# _ prefixed file name defines an internal, meta 
# a filename in all-capitals is mutable, and intended to be updated/rewritten as development progresses
# _RULES.md files are cascading, each child directory should contain one and inherit the rules from it's parent directory's _RULES.md