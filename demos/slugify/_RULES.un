# Slugify rules (inherits from ../../undefined/_RULES.un)

1. Inheritance
  - Inherit all rules from `../../undefined/_RULES.un`.

2. Entry point
  - Entry file: `_MAIN.un` in this directory.

3. Execution policy
  - Inline transforms with `()`; no external calls.
