# Docker image check workflow

1. Read purpose
2. Initialize state (return { state: {}, priorReturn: null })
3. For each function
  a. Resolve inputs
  b. Execute docker inspect; capture exit code and output
  c. Validate outputs; set `exists`
  d. Return and update
  e. Log
  f. TODO
4. Completion
